plugins {
    id 'java'
    id 'application'
    id 'org.graalvm.buildtools.native' version '0.11.5'
    id 'com.gradleup.shadow' version '9.4.3'
}

group = 'io.ajcm'
version = '2.0.1'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(25)
    }
}

repositories {
    mavenCentral()
}

dependencies {
    // SDK 2.0.1 requires local STDIO concurrency hardening; see
    // ResilientStdioServerTransportProvider. Re-evaluate it on the next SDK upgrade.
    implementation platform('io.modelcontextprotocol.sdk:mcp-bom:2.0.1')
    implementation 'io.modelcontextprotocol.sdk:mcp-core'
    implementation 'io.modelcontextprotocol.sdk:mcp-json-jackson2'
    implementation 'net.sf.jt400:jt400:21.0.7'
    implementation 'com.singlestore:singlestore-jdbc-client:1.2.12'
    runtimeOnly 'org.slf4j:slf4j-simple:2.0.18'
    
    testImplementation platform('org.junit:junit-bom:6.1.3')
    testImplementation 'org.junit.jupiter:junit-jupiter'
    testImplementation 'io.modelcontextprotocol.sdk:mcp-test'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

test {
    useJUnitPlatform()
}

application {
    mainClass = "io.ajcm.multidb.mcp.Main"
}

tasks.withType(Jar).configureEach {
    manifest {
        attributes('Implementation-Version': project.version)
    }
}

//// Configure shadowJar to merge JDBC driver services
//shadowJar {
//    mergeServiceFiles()
//}
